dependencies:
  override:
    - camus-shopify/script/setup

deploy:
  override:
    - mvn clean package
    - bash -c ". .venv/bin/activate && camus-shopify/script/upload && camus-shopify/script/update_schedule"
